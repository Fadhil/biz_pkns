= form_for @business_category, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label t('name'), :class => 'control-label'
    .controls
      = f.text_field :name, :class => 'text_field'
  .form-actions
    - if f.object.new_record?
      = f.submit t('.add_new_business_category'), :class => 'btn btn-primary'
    - else
      = f.submit t('update'), class: 'btn btn-primary'
    = link_to t('cancel', :default => t("helpers.links.cancel")), business_categories_path, :class => 'btn'
