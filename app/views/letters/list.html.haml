- model_class = Letter
.row
  %h3.eight.columns
    = t('.title')
  .four.columns.action
    %span.pretty.medium.primary.btn.entypo.icon-left.icon-list-add
      = link_to t('.new'), new_letter_path
/ %center
/   .container
/     %dl.dl-horizontal
/       %div.adbox
/         - @adverts.each do |advert|
/           - if advert.photo
/             - if !advert.photo.file.nil?
/               %div.adinfo.userselect
/                 %div.title= advert.title
/                 %div.venue= 'Tempat : '+ advert.location.to_s
/                 %div.date= 'Tarikh : '+ advert.start_date.strftime("%d %b %y").to_s
/               = image_tag (advert.photo.file)#, :class => 'adboxw'#, size: '100x100'
%table.table.table-striped
  %thead
    %tr
      %th= "Bil"
      %th= t('.subject')
      %th= t('.created_at')
      %th
  %tbody
    - @letters.each_with_index do |letter, i|
      %tr
        %td= i+1
        %td= letter.subject
        %td= letter.created_at.strftime("%d %b %y, %H:%M")
        %td
          =# link_to t('.edit', :default => t("helpers.links.edit")), edit_newsletter_path(newsletter), :class => 'btn btn-mini'
          %span.small.pretty.btn.primary
            = link_to t('.preview'), preview_letter_path(letter)#, :class => 'btn btn-mini'
          =# link_to t('.destroy', :default => t("helpers.links.destroy")), newsletter_path(newsletter), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

