= form_for @education, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label t('.name'), :class => 'control-label'
    .controls
      = f.text_field :name, :class => 'text_field'
 
  .form-actions
    = f.submit t('.new'), :class => 'btn btn-primary'
