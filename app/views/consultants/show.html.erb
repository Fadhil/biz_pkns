<%- model_class = Consultant -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<dl class="dl-horizontal">
  <div>
    <% if !@consultant.profile_photo.nil? %>
      <%= image_tag (@consultant.profile_photo.file) %>
    <% end %>
  </div>
  <dt><strong><%= t('first_name') %>:</strong></dt>
  <dd><%= @consultant.first_name %></dd>
  <dt><strong><%= t('last_name') %>:</strong></dt>
  <dd><%= @consultant.last_name %></dd>
  <dt><strong><%= t('program') %></strong></dt>
  <dd><%= @consultant.program.name unless @consultant.program.nil? %></dd>
  <% unless current_user %>
    <% if @consultant.phone.blank? %>
    <% else %>
      <dt><strong><%= t('phone') %>:</strong></dt>
      <dd><%= @consultant.phone %></dd>
    <% end %>
    <dt><strong><%= t('email') %>:</strong></dt>
    <dd><%= @consultant.email %></dd>
  <% end %>
</dl>

<% if current_user %>
  <div class='row'>
    <div class='span4'>
      <h3><%= t('bio_data') %></h3>
      <p><%= @consultant.bio_data %></p>
    </div>
  </div>
<% end %>


<div class="form-actions">
  <%= link_to t('back', :default => t("helpers.links.back")),
              consultants_path, :class => 'btn'  %>
  <% unless !current_user %>
    <%= link_to t('edit', :default => t("helpers.links.edit")),
                edit_consultant_path(@consultant), :class => 'btn' %>
    <%= link_to t('destroy', :default => t("helpers.links.destroy")),
                consultant_path(@consultant),
                :method => 'delete',
                :data => { :confirm => t('confirm', :default => t("helpers.links.confirm", :default => t('are_you_sure'))) },
                :class => 'btn btn-danger' %>
    <%= link_to t('contact'), contact_consultant_path(@consultant), :class => 'btn btn-info' %>
  <% end %>
</div>